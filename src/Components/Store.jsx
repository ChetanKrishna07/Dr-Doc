import React from 'react'
import { Accordion, CustomToggle } from 'react-bootstrap'

const Store = (props) => {
    
        const headerStyles = {

        }

        return (
            <Accordion>
                
            </Accordion>
        )
    }

    export default Store